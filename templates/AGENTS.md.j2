# {{ project_name }}

> {{ project_description }}

## Quick Start

```{% if stack == "rust" %}bash
cargo build                    # build
cargo test                     # full suite — target ≤60s
cargo clippy -- -D warnings    # lint — zero warnings policy
cargo fmt --check              # format check{% elif stack == "node" %}bash
npm install                    # install dependencies
npm test                       # full suite — target ≤60s
npm run lint                   # lint{% elif stack == "python" %}bash
pip install -e ".[dev]"        # install with dev deps
pytest                         # full suite — target ≤60s
ruff check .                   # lint{% elif stack == "go" %}bash
go build ./...                 # build
go test ./...                  # full suite — target ≤60s
go vet ./...                   # lint{% else %}bash
# TODO: Add your build, test, and lint commands here{% endif %}
```

## Architecture

See [ARCHITECTURE.md](ARCHITECTURE.md) for the domain map, layer structure, and dependency rules.

## Workflow

1. **Orient.** Read the active plan or sprint in [`docs/exec-plans/active/`](docs/exec-plans/active/). Run the test suite to confirm the tree is green.
2. **Know the quality bar.** Read [`docs/evaluation/criteria.md`](docs/evaluation/criteria.md). Target grade B or above.
3. **Respect the architecture.** Modules have strict dependency direction — see [ARCHITECTURE.md](ARCHITECTURE.md). Dependencies flow in one direction only.
4. **Validate before handing off.** Run the full check suite (tests, lint, format). Fix any failures before declaring work complete.
5. **Record decisions.** Non-obvious choices go in the execution plan's Decision Log or in [`docs/design-docs/`](docs/design-docs/).

## Key Constraints

- **Type safety.** Use build-time type checking with end-to-end coverage. Validate external input into typed representations at system edges — parse, don't validate.
- **No silent failures.** Every error path produces a user-visible message with what happened and what to do.
- **Dependency direction.** Dependencies flow forward only — no backward imports between layers. See [ARCHITECTURE.md](ARCHITECTURE.md).
- **Test speed.** Test suite must complete in ≤60 seconds. Fast tests keep the feedback loop tight.
- **Stop on ambiguity.** When encountering destructive operations, unclear requirements, or security-sensitive changes, report what you found and wait for guidance rather than proceeding.

## Documentation Map

**Implementation docs** — read these for every task:

| Topic | Location |
|-------|----------|
| Architecture & layers | [ARCHITECTURE.md](ARCHITECTURE.md) |
| Core beliefs | [docs/design-docs/core-beliefs.md](docs/design-docs/core-beliefs.md) |
| Evaluation criteria | [docs/evaluation/criteria.md](docs/evaluation/criteria.md) |

**Reference & workflow** — consult when needed:

| Topic | Location |
|-------|----------|
| Design decisions | [docs/design-docs/](docs/design-docs/) |
| Active plans | [docs/exec-plans/active/](docs/exec-plans/active/) |
| Completed plans | [docs/exec-plans/completed/](docs/exec-plans/completed/) |
| Workflow templates | [docs/templates/](docs/templates/) |

## Tooling

This project uses [`harn`](https://github.com/imsuven/harn) for harness lifecycle management.

| Command | Purpose |
|---------|---------|
| `harn check` | Validate harness structure integrity |
| `harn plan new "description"` | Create an execution plan |
| `harn sprint new "description"` | Create a sprint contract |
| `harn sprint done` | Complete current sprint |
| `harn status` | Show current project state at a glance |
| `harn gc` | Detect stale documentation |
| `harn score update` | Assess and record quality grades |
